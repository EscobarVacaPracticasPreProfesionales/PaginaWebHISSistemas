$('#tablaContacto').html("<%= escape_javascript(render 'tablecontact', contacts: @contacts )%>")
$('#tablaUsuario').html("<%= escape_javascript(render 'tableusers', users: @users )%>")

$('[data-toggle="tooltip"]').tooltip();

$('[data-toggle="tooltip"]').on('click', function () {
    $(this).tooltip('hide')
}) 
