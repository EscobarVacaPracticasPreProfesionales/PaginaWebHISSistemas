$('#tablaContacto').html("<%= escape_javascript(render 'tablecontact', contacts: @contacts )%>")
$('#tablaCliente').html("<%= escape_javascript(render 'tablecustomer', customers: @customers )%>")

$('[data-toggle="tooltip"]').tooltip();

$('[data-toggle="tooltip"]').on('click', function () {
    $(this).tooltip('hide')
}) 
