<div class="container">
	<h1 class="py-3"><%=t(".new_service")%></h1>	
	<div class="row">
		<div class="col-12 col-md-7 col-lg-6">
			<%= render 'form', service: @service %>
      		<%= link_to '<i class="fas fa-angle-left"></i>'.html_safe, services_path, class: "btn btn1 mt-3"%>
		</div>
		<div class="col-12 col-md-6">
			<% if @service.pictures.any? %>
				<div class="row" id="result">
					<%=render 'images'%>
				</div>
			<%end%>
		</div>
	</div>
</div>