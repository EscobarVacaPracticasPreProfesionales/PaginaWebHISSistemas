<% if flash[:errors].present? %>
  <div id="error_explanation" class="p-2">
    <span>
      <%= I18n.t("errors.messages.not_saved",
                 count: flash[:errors].count,
                 resource: resource.class.model_name.human.downcase)
       %>
    </span>
    </h5>
    <ul>
      <% flash[:errors].each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
